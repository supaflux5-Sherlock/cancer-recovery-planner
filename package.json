{
  "name": "cancer-recovery-planner",
  "version": "1.0.0",
  "private": true,
  "description": "Android packaging for the Cancer Recovery Planner web app.",
  "scripts": {
    "build": "node scripts/build-www.js",
    "sync": "npm run build && cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  }
}
